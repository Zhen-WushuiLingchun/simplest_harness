# DeepSWE v1.1 跨仓库 20 题：preflight 证据

状态：**Oracle、Nop 与动态 adapter gate 均已通过；正式模型 trial 未启动。**

## Oracle gate

- Job：`crossrepo20-oracle-prereg-v1-20260812`
- 时间：`2026-08-12T18:32:00.141001` 至 `2026-08-12T18:42:09.755306`
- Job wall time：`609.614305 s`
- 完成：`20/20`
- Pier exceptions：`0`
- Reward：`20×1.0`，总和 `20`
- F2P：`50.05/50.05=1.0`（Pier job aggregation）
- P2P：`1208.3/1208.3=1.0`（Pier job aggregation）
- Partial：`1.0`
- `result.json` SHA-256：
  `70080B6B2E3B56376B9909B8CDA1264131F52506C1F5DE2FFBB274145DC3973B`

从 20 个 trial `result.json` 的整数 test 计数相加得到 F2P
`1001/1001`、P2P `24166/24166`。Pier 的 job summary 对分母做了 task-level
归一化后再相加，因此显示 `50.05` 和 `1208.3`；两种表示都保留，不能互相冒充。

## Nop gate

- Job：`crossrepo20-nop-prereg-v1-20260812`
- 时间：`2026-08-12T18:43:54.755420` 至 `2026-08-12T18:51:15.609986`
- Job wall time：`440.854566 s`
- 完成：`20/20`
- Pier exceptions：`0`
- Reward：`20×0.0`，总和 `0`
- F2P：`0.0/50.05=0.0`（Pier job aggregation）
- P2P：`1208.3/1208.3=1.0`（Pier job aggregation）
- Partial mean：`0.6426343143165926`
- `result.json` SHA-256：
  `F6FC05533E7DB6C996A3059A021800EF6E324037AD0A7D732E6CC37495234220`

从 20 个 trial `result.json` 的整数 test 计数相加得到 F2P `0/1001`、P2P
`24166/24166`。所有 base checkout 都缺少目标功能，没有空补丁 Reward `1`。

## Adapter 与 endpoint gate

- `GET https://api.deepseek.com/models` 在不打印 Key 的情况下返回 HTTP `200`；账户
  可见 model 数 `2`，其中包含 `deepseek-v4-flash` 与 `deepseek-v4-pro`。
- Adapter 把 agent Node 从浮动 `nvm install 24` 改为精确
  `nvm install 24.19.0`，并在 Pier install verification 中验证 Node
  `v24.19.0`、pnpm `11.21.0` 和 TMSH commit。
- 初始无 cache telemetry 的动态 smoke 冻结 TMSH
  `33cf1b975f97e77f87868a2189758d8dd4127520`；完整证据保留在下文。
- 正式实验根据 amendment 1 改为纯观测 commit
  `709ed7905aea0ac95d4d9c5d094a7e1c9f0e406a`。
- `pier_tmsh_agent.py` SHA-256：
  `B14DEE3029A3FA05E1B0D8679DCDDA8A614ECA75AFA141A69F2EE2595E3A6048`
- `pier_tmsh_direct_agent.py` SHA-256：
  `D4B75AB198E133C014E5C288D51CA9C8A8494C2708985828EF39D0FF53715011`
- `run-deepswe-crossrepo20.ps1` SHA-256：
  `9442AD50CE90A595D371D6F8C6E31D1884F47BB4AEBF1F47C49AB1FA2370B654`
- `pier_tmsh_install_smoke_agent.py` SHA-256：
  `F4248D27426CB18C4787BADFFF43326FD844B1ED06252BAB81F83399E40DB623`
- Python `ast.parse`：`3/3` 文件通过。
- PowerShell parser：`0` errors。

动态 smoke 使用 `TmshInstallSmokeAgent` 构建与正式 adapter 相同的 install spec，随后
禁用 verifier 并只检查版本；它没有读取 Key、没有发送模型请求。初始 smoke 为：

- Job：`crossrepo20-adapter-build-smoke-v1-20260812`
- 时间：`2026-08-12T10:55:16.629882Z` 至 `2026-08-12T10:56:23.497475Z`
- Job 完成：`1/1`，errors `0`，`exception_info=null`
- Node：`v24.19.0`
- pnpm：`11.21.0`
- TMSH：`33cf1b975f97e77f87868a2189758d8dd4127520`
- `model_called=false`
- input/cache/output/cost：均为 `null`
- Trial `result.json` SHA-256：
  `A17AE9939C285E46BA17131FB9912C303942AFE11737CCC66DBDE12BCCB44DC4`
- Job `result.json` SHA-256：
  `ED9F4748A2F41A1A04067094DDFFD87E52DDD95FBC6CE78DFD7C8686CC52C6AB`

由于 verifier 被有意禁用，Pier eval 表显示 scored `Trials=0`；这不是模型 Reward
`0`，也不进入任何能力分母。Pier job 层仍记录 `n_completed_trials=1`、
`n_errored_trials=0`。

cache telemetry amendment 后重新构建，没有复用旧 TMSH commit：

- Job：`crossrepo20-adapter-cache-smoke-v1-20260812`
- 时间：`2026-08-12T11:01:19.854382Z` 至 `2026-08-12T11:02:24.811082Z`
- Job 完成：`1/1`，errors `0`，`exception_info=null`
- Node：`v24.19.0`
- pnpm：`11.21.0`
- TMSH：`709ed7905aea0ac95d4d9c5d094a7e1c9f0e406a`
- `model_called=false`
- input/cache/output/cost：均为 `null`
- Trial `result.json` SHA-256：
  `C17F5E4825975D28CEC12AE345A398E7AD8B0EB471C9A9024854CA091D6103EA`
- Job `result.json` SHA-256：
  `F8A2E7DD57C92A4FDC3BF4DCD74BC912072ADFAD47766723ED66E2046F865529`

随后进行两个不属于 benchmark 分母的真实 telemetry smoke。两次 prompt 相同，均只
返回精确文本 `CACHE_OK`，model request/response 各 `1/1`、工具调用/结果 `0/0`、
TMSH 终态 `done`：

| Run                                    |  Input | Cache read | Cache miss | Output | 按冻结价格计算费用 |
| -------------------------------------- | -----: | ---------: | ---------: | -----: | -----------------: |
| `ce658a3b-1c1a-4a25-a5f3-c46d9a2b071e` | `2510` |     `2432` |       `78` |   `55` | `0.0000331296 USD` |
| `c7a90013-cf80-46ac-8cca-250184b5ef52` | `2510` |     `2432` |       `78` |   `54` | `0.0000328496 USD` |

两次合计计算费用 `0.0000659792 USD`。这些字段直接来自 DeepSeek response，而不是由
input 减法估计；测试同时证明 AI SDK aggregate usage 会丢弃 raw provider 字段，TMSH
必须从 single final step 读取。两个 smoke 在本机 Node `v24.8.0` 执行，只验证字段
passthrough；正式 runtime 的 Node `v24.19.0` 已由上面的无模型 install smoke 独立验证。

正式启动前还验证了分母语义：精确的 `model call budget exhausted: 200`、明确 context
limit 和 `5400 s` agent execution timeout 会保留当前 worktree 并继续 verifier，计作
harness Reward `0`；其他非零退出仍抛出 Pier exception。分类 helper 断言 `3/3`
通过。终态 metadata 另外保留 `terminal_status` 与 `terminal_error`，避免把资源耗尽
写成普通完成。

## 保留的失败尝试

Oracle 的 Pier job 本身成功，但 runner v1 的 terminal validator 误把一个 eval 下的
聚合 metrics 对象当成 20 个 trial metrics，输出：

`CAMPAIGN_TERMINAL phase=Oracle job=crossrepo20-oracle-prereg-v1-20260812 status=error metrics=1/20 unexpected_reward=0`

该假阴性不属于 task/oracle 失败。修复方式不是重跑，而是按
`reward_stats.reward` bucket 中的 trial ID 数验证；对同一现有 `result.json` 使用
`-ValidateOnly` 得到：

`CAMPAIGN_TERMINAL phase=Oracle job=crossrepo20-oracle-prereg-v1-20260812 status=complete completed=20/20 errored=0`

第一次从 PowerShell 运行 `python -m py_compile` 失败，Windows Python launcher 实际
不可用，进程返回 `9009`；随后用 WSL Python `ast.parse` 验证成功。一次只读 WSL
Docker image 查询还报告
`Failed to start the systemd user session for 'hydro'`；它不影响同一 WSL distro 下
此前已经完成的 Pier Oracle/Nop jobs，但作为本机 WSL 状态风险保留。

## 尚未完成的工作和下一步

全部门禁与 cache telemetry smoke 已经通过。尚未启动正式 `80` 个模型 trial。

精确下一步：以前台阻塞、`-Quiet`、并发 `4` 启动 `20×4` 正式 campaign；只在
`CAMPAIGN_TERMINAL` 完成或错误时反馈。随后按预注册逐题比较公开 `33/80`，不得因
任何 TMSH reward 改换任务。
